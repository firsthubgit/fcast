name: pall
description: Cast for all platforms;
version: 1.0.0+1

environment:
  sdk: ">=2.3.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^0.1.2

  # 
  url_launcher:
  menubar:
    path: ../../../flutter/flutter-desktop-embedding/plugins/menubar
  shared_preferences:
  pointycastle:
  asn1lib:
  convert:
  device_info:


dev_dependencies:
  flutter_test:
    sdk: flutter
  test:

flutter:
  uses-material-design: true
